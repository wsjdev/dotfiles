" plugin/ 以下の設定ファイルをすべて読み込む
set runtimepath+=$ZSH/vim/
runtime! plugin/vimrc.plugin.*

" Rubyのオムニ補完を設定
let g:rubycomplete_buffer_loading = 1
let g:rubycomplete_classes_in_global = 1
let g:rubycomplete_rails = 1

set completeopt=menuone
